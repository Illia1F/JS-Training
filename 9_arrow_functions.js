/**Arrow Functions
 * 
 * Arrow functions were introduced in ES6.
 */
function foo(a, b, c) {
  return a + b + c;
}

const foo2 = (a, b, c) => {
  return a + b + c;
};

const foo3 = (a, b, c) => a + b + c;
